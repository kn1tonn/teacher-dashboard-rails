<section class="space-y-6">
  <header class="flex flex-col gap-2 md:flex-row md:items-center md:justify-between">
    <div>
      <h1 class="text-2xl font-semibold text-slate-900">Submissions</h1>
      <p class="text-sm text-slate-600">Filter and manage work awaiting AI checks or teacher review.</p>
    </div>
    <%= link_to "Back to dashboard", dashboard_path, class: "text-sm font-medium text-indigo-600 hover:text-indigo-500" %>
  </header>

  <div class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm">
    <%= form_with url: teacher_submissions_path, method: :get, local: true, class: "grid gap-4 md:grid-cols-4" do %>
      <div class="space-y-1">
        <%= label_tag :status, "Status", class: "text-xs font-medium uppercase tracking-wide text-slate-500" %>
        <%= select_tag :status,
                       options_for_select([["All", ""]] + Submission.statuses.keys.map { |status| [status.humanize, status] }, @status_filter),
                       class: "w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200" %>
      </div>
      <div class="space-y-1">
        <%= label_tag :task_id, "Task", class: "text-xs font-medium uppercase tracking-wide text-slate-500" %>
        <%= select_tag :task_id,
                       options_for_select([["All", ""]] + @tasks.map { |task| [task.title, task.id] }, @task_filter),
                       class: "w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200" %>
      </div>
      <div class="space-y-1">
        <%= label_tag :week, "Week", class: "text-xs font-medium uppercase tracking-wide text-slate-500" %>
        <%= week_field_tag :week, @week_filter,
                           class: "w-full rounded-lg border border-slate-300 px-3 py-2 text-sm focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-200" %>
      </div>
      <div class="flex items-end justify-end gap-2">
        <%= link_to "Reset", teacher_submissions_path, class: "rounded-lg border border-slate-300 px-3 py-2 text-sm text-slate-600 hover:text-slate-800" %>
        <%= submit_tag "Apply", class: "rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white transition hover:bg-slate-700" %>
      </div>
    <% end %>
  </div>

  <div class="overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm">
    <table class="min-w-full divide-y divide-slate-100 text-sm">
      <thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">
        <tr>
          <th class="px-5 py-3">Student</th>
          <th class="px-5 py-3">Task</th>
          <th class="px-5 py-3">Status</th>
          <th class="px-5 py-3">Updated</th>
          <th class="px-5 py-3 text-right">Action</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-slate-100">
        <% if @submissions.blank? %>
          <tr>
            <td colspan="5" class="px-5 py-4 text-center text-slate-500">No submissions match the selected filters.</td>
          </tr>
        <% else %>
          <% @submissions.each do |submission| %>
            <tr class="hover:bg-slate-50">
              <td class="px-5 py-4 font-medium text-slate-800"><%= submission.user.name %></td>
              <td class="px-5 py-4 text-slate-600"><%= submission.task.title %></td>
              <td class="px-5 py-4"><%= submission_status_badge(submission.status) %></td>
              <td class="px-5 py-4 text-slate-500"><%= submission.updated_at.strftime("%b %-d, %Y %H:%M") %></td>
              <td class="px-5 py-4 text-right">
                <%= link_to "Review", teacher_submission_path(submission), class: "text-sm font-semibold text-indigo-600 hover:text-indigo-500" %>
              </td>
            </tr>
          <% end %>
        <% end %>
      </tbody>
    </table>
  </div>
</section>
